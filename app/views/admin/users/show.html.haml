.page-header
  用户信息

%p#notice= notice

%p
  %b Username:
  = @user.username
%p
  %b Email:
  = @user.email
%p
  %b Is admin:
  = @user.is_admin

= link_to '修改', edit_user_path(@user), :class => 'btn btn-mini btn-primary'
\|
= link_to '返回', users_path, :class => 'btn btn-mini'
